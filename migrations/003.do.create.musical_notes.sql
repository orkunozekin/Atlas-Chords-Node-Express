CREATE TABLE musical_notes (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    string INTEGER NOT NULL,
    fret INTEGER,
    finger INTEGER,
    strummed BOOLEAN NOT NULL,
    chord_id INTEGER REFERENCES chords(id) ON DELETE CASCADE NOT NULL
);

